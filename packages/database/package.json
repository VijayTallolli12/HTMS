{
  "name": "@hms/database",
  "version": "1.0.0",
  "description": "PostgreSQL database connection pool and schema readiness baseline for Enterprise HMS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b --force",
    "clean": "rimraf dist",
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=prisma/schema.prisma",
    "prisma:deploy": "prisma migrate deploy --schema=prisma/schema.prisma",
    "seed": "ts-node src/seed/seed-demo.ts",
    "seed:reset": "echo WARNING: seed:reset DESTROYS local database. NEVER run against production. && prisma migrate reset --force --schema=prisma/schema.prisma && npm run seed"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "pg": "8.11.5"
  },
  "devDependencies": {
    "@types/pg": "8.11.6",
    "prisma": "5.22.0",
    "typescript": "5.4.5"
  }
}
